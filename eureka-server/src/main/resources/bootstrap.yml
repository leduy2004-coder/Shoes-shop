spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      fail-fast: false
      label: main
  config:
    import: optional:configserver:${CONFIG_SERVER_URI}