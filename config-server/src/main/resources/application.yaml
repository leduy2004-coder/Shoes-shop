server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "" # Tắt Git bằng cách để trống
        native:
          search-locations: file:E:/SpringBoot/microservice/Shoes-shop/config-server/config-repo
  profiles:
    active: native